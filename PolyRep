#include <stdio.h>
struct
	{
		int ex;
		int co;
	}ar[10];

int main()
{
int c=0,pow=0,i=0,j=0;
	printf("Enter the Highest Ranking Power : \n");
	scanf("%d",&pow);
	printf("Enter the Cofficient of X (Starting with the Costant): \n");
		for(i=0;i<=pow;++i)
			{
				
				scanf("%d",&ar[i].co);
				ar[c++].ex=i;
			
			}
			
		printf("%d",ar[0].co);
			for(i=1;i<=pow;i++)
				{
				printf("+%dx^%d",ar[i].co,ar[i].ex);
				}
}
