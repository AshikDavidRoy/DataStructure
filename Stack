#include <stdio.h>
void push(int);
void pop();
void display();

int stack[10],top = -1;

int main()
{
	int v,c;
	
	while(1)
	{
		printf("The choice : \n");
		printf("1. Push \n 2.POP \n");
		printf("Your choice : \n");
		scanf("%d",&c);
		switch(c)
		{
			case 1 : 
			printf("push the element\t");
			 break;
			case 2 : 
			printf("POP");
			 break;
		}
		 break;
	}
}
